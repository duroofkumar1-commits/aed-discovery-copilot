# Data Manifest

## Primary dataset (development sample)

`data/aed_sample.geojson`, generated by `src/generate_dataset.py`.

| | |
|---|---|
| Source | 13 real records from the official dataset-preview sample on the "Public Access AEDs" page, `SCDF, Singapore Civil Defence Force`, data.gov.sg |
| Source URL | https://data.gov.sg/datasets/d_4e6b82c58a8a832f6f1fee5dfa6d47ea/view |
| Retrieval date | 9 Aug 2026 |
| Licence | Singapore Open Data Licence — free for personal or commercial use with attribution; must not imply official endorsement |
| Underlying data date (per source page) | Feb 2020, page last updated 13 Nov 2025 |
| Citation | Singapore Civil Defence Force. (2025). *Public Access AEDs (2025)* [Dataset]. data.gov.sg. |

**This prototype could NOT reach the live `api-open.data.gov.sg` download
endpoint from its build environment** (network egress was restricted to a
package-registry allowlist). Only the ~13-record preview sample embedded
in the dataset's own webpage was retrievable. The remaining ~172 records
in `aed_sample.geojson` are **synthetic** — generated by
`generate_dataset.py` to exercise the ranking logic at a realistic
national scale (real Singapore neighbourhood names/coordinates, plausible
building types and operating-hours text, but fabricated building names,
AED IDs, and exact addresses).

Every feature in the GeoJSON carries a `SOURCE` property:
- `"real_sample"` — one of the 13 verbatim preview records
- `"synthetic"` — generated for development only

**Before submitting to the actual hackathon**, replace this file with the
organizer-frozen copy (or a fresh `GEOJSON` download from the dataset
page) and re-run `src/evaluate.py`. The ranking and parsing code in
`src/` does not depend on which records are real vs. synthetic, so this
is a data swap, not a code change — just update `DATA_PATH` in
`src/evaluate.py` / `web/app.js`'s `DATA_URL` if the filename changes.

## What this dataset does not contain

Per the challenge brief, the supplied AED dataset does not include: live
device status (battery, pad expiry, inspections), real-time closures or
access restrictions, emergency incident or dispatch data, population or
footfall data, or ownership contact information. This prototype makes no
claims beyond what a static registry snapshot and its text fields
support.

## No other external data was used

The prototype uses no third-party APIs, pretrained models, or additional
datasets. Map tiles are drawn at runtime from CARTO's public dark
basemap (OpenStreetMap data, `{s}.basemaps.cartocdn.com`), which is
display-only and not part of the evaluated dataset.
